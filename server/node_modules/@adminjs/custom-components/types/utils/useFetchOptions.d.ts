import { RecordJSON, PropertyJSON } from 'adminjs';
export type ReferenceFieldFilterType = StringFilter | ReferenceFilter | OwnerFilter;
interface StringFilter {
    type: 'string';
    value: string;
}
interface ReferenceFilter {
    type: 'reference';
    field: string;
}
interface OwnerFilter {
    type: 'owner';
    source?: string;
}
interface Options {
    record: RecordJSON;
    property: PropertyJSON;
    resourceId: string;
    searchProperty: string;
    filters: Record<string, ReferenceFieldFilterType>;
    selectedIds: string[];
}
export declare const useFetchOptions: ({ record, resourceId, searchProperty, filters, selectedIds, }: Options) => {
    search: (input: string) => Promise<RecordJSON[]>;
    error: string | null;
    selected: RecordJSON[];
};
export {};
